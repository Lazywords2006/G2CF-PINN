# 独立代码复审结论

复审日期：2026-07-29  
范围：dual-path 复 MGS、accelerator 采样、true Ky Fan trace、resume/finalize、冻结 suite evaluator、参考缓存、统计与 smoke decision provenance。

## 最终结论

| 严重度 | 未关闭问题 |
|---|---:|
| Critical | 0 |
| Major | 0 |
| Minor | 0 |

**工程判定：scientific CUDA pilot GO。** 这只授权 harmonic + Gaussian、3 seed、小预算 CUDA pilot；不授权直接启动 10-seed formal，也不表示论文可以投稿。

## 已独立验证

- `45/45` 自动测试通过；
- 真实可用 accelerator 上的 CPU-generator 坐标采样不再发生 device mismatch；
- dual-path 参数梯度与有限差分一致；
- partial resume 保留原始 initial loss；完成态即使缺 `run_summary.json` 或 `final.pt` 也可确定性收尾；
- evaluator 对当前协议校验 config/source fingerprint；
- 参考缓存严格绑定 suite hash、cutoff、grid、ID、完整 basis/eigenvalue 形状和 finite；
- 新训练 summary 保存逐 seed final-checkpoint SHA，decision builder 将评价 checkpoint 与训练 checkpoint/config/source 精确绑定；
- 缺 seed、点集子集、重复 checkpoint、跨臂复用、错误 config/source、篡改 checkpoint SHA 均 fail closed；
- 旧 2026-07-29 归档只有显式 `--allow-legacy-unbound-checkpoints` 才能重建，并在输出中标为 `legacy_unbound_explicit`；新 pilot 禁止该参数；
- supervised checkpoint 同时绑定库源码、trainer 源码、dataset、config、seed 与 sampling RNG。

## 最终持久化工程探针

`results/code_integrity_trace_smoke_v4_20260729/` 使用最终复审源码运行 seed 910、10 steps、12 个 harmonic V2 点。它的 training final-checkpoint SHA 与 evaluator checkpoint SHA 均为：

`96ce8496a84b47ebdc3be52b1634090ba5a6f1973b8f87c4dd9875e414b17c2b`

config/source/checkpoint 三类指纹精确匹配。该探针只证明证据链闭环，不用于精度结论。

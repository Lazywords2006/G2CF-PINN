# 2026-07-29 目录整理记录

## 整理结果

- 第一轮将工作区由约 `4.8 GiB` 降至约 `2.4 GiB`，第二轮进一步降至 `1.8 GiB`；
- 顶层目录由两个冲突的 `04_` 和多个临时输出目录，整理为连续的 `00`、`01`、`03`、`04`、`05`、`06`；
- 从当前实验工程导出独立代码仓库 [`Lazywords2006/PINN-PDE`](https://github.com/Lazywords2006/PINN-PDE)，首个提交为 `2e0b990`；远端包含 107 个文件、约 `418 KiB` Git blob，不携带旧 G2CF Git 历史、文献 PDF 或实验产物；
- 旧工程已经从当前工作区移除；系统废纸篓只提供短期误删恢复能力，不能把它当作旧工程备份；
- 未执行 `git reset`、`git clean`，未删除当前主线源码、checkpoint、文献或最终实验归档。

恢复边界：Git 曾跟踪的旧文件仍可从 Git 历史恢复；2026-07-29 第二轮清理项已移入 macOS 废纸篓、尚未清空，当前仍可恢复，但废纸篓不能作为长期备份。当前主线所需源码、真实结果、checkpoint、文献和哈希均仍在工作区。

## 保留

- 当前二维 Block KyFan-PINN 的源码、配置、测试和依赖清单；
- 当前主线 RTX 4090、Apple MPS、CPU 的展开结果、checkpoint、日志、参考缓存、哈希和最终带图归档；
- 170 份本地 PDF、BibTeX、75 篇核心矩阵、19 篇 2026 增量矩阵与 SHA-256；
- 2026-07-29 最新深度评估、开题、论文提纲、初稿、正式实验与期刊证据链；
- 旧路线的最终 STOP/REVISE 判定和 G2CF 小型汇总表。

## 从工作区移出

- 完整停止路线工程：TSONN、LLC-HFNO、PeSDF、PeR-VPINN、G2CF-PINN、1D A-KyFanNet 和 FalsifyPDE；
- 旧路线 `.venv`、checkpoint、可再生成数据和逐步日志；
- 2026-07-12/13 的 V1 清单、旧初稿、旧答辩页和旧上卡手册；
- 根目录 `output/`、`outputs/`、`tmp/`、Playwright 快照、会话缓存和 ARIS 二进制；
- 旧 G2CF 数据分析和含占位 arXiv 编号的无效搜索清单；
- 当前实验工程的 `.venv`、Python 缓存和 pytest 缓存；
- Mian、Fanaskov、NeuralSVD 的第三方仓库副本及其嵌套 `.git`，改为保留官方地址、许可证和固定提交索引；
- 许可证不明确的 Wang–Xie 作者压缩包，不再分发；
- 已被 `block_kyfan_results_final_with_figures.tgz` 覆盖的六个阶段性 RTX `.tgz`；最终归档 SHA-256 `22b82e13410daf5e6df2822afa50aaa719fff0c69635a98c0c818fea21905644` 保留；
- `.DS_Store` 和其他可再生成缓存。

## 目录含义

| 目录 | 作用 |
|---|---|
| `00_项目总览` | 通俗 HTML 和本次整理记录 |
| `01_文献库` | 论文原文、核心文献包、BibTeX 与哈希 |
| `03_当前主线_BlockKyFanPINN` | 唯一研究代码、测试和真实结果 |
| `04_SCI三区投稿准备` | 最新证据包、开题、初稿与正式实验计划 |
| `05_历史路线摘要` | 旧路线最终结论，不含大型训练工程 |
| `06_投稿筛查` | 投稿风险辅助名单，正式投稿前需更新 |

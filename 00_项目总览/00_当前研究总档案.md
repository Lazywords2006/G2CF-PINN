# 当前研究总档案

更新日期：2026-08-24
适用范围：研究决策、实验执行、论文写作和后续 AI 交接的唯一主文档。

## 1. 一句话结论

课题仍可继续，且属于真实的“神经网络求解二维偏微分本征方程”；但是 P5 已经否定
“无条件低频 ROM 是核心创新”。独立 P0 风险开发已经 GO，证明组合无标签量能在 held-out
参数上识别 ROM 退化；当前工作处于**风险可检测性完成、条件校正器尚未设计**的阶段。
下一步不是立即烧 GPU，而是冻结 P1 条件校正器设计和公平小测协议。

当前不能投稿，frozen final 继续关闭。

## 2. 到底使用什么网络，求解什么方程

### PDE

求解二维参数化 Bloch–Schrödinger 本征偏微分方程：

\[
\left[\frac12(-i\nabla+\mathbf{k})^T G(-i\nabla+\mathbf{k})
+V_\mu(\mathbf{x})\right]u_j(\mathbf{x})
=E_j u_j(\mathbf{x}),
\qquad \mathbf{x}\in[0,2\pi)^2,
\]

并满足二维周期边界条件。研究两个周期势族：harmonic honeycomb 与 Gaussian
honeycomb。

### 网络

已经稳定实现的底座是：

- 周期坐标编码的轻量 SiLU MLP；
- K 点附近的低能物理 anchor；
- generalized-trace / Ky Fan 无标签变分目标；
- dual-path 复正交化；
- 自动微分、数值求积和 rank-2 projector 评价。

P5 候选 A-GTROMNet 另外加入七个低频 Fourier ROM 系数。这个附加机制已经被公平
控制实验判定 STOP，因此它不是当前最终网络，也不能写成论文主贡献。

### 输出对象

网络学习最低两个本征态共同张成的 rank-2 谱簇。两条能带在 Dirac 点内部相交时，单个
本征函数可以交换编号或在簇内旋转；projector 与 principal angle 不依赖这种任意基选择，
因此比逐带标签更合理。

## 3. 当前到底进行到哪一步

| 阶段 | 状态 | 真实结论 |
|---|---|---|
| A. 问题与 PDE 选择 | 已完成 | 二维 Bloch–Schrödinger rank-2 谱簇，课题未跑题 |
| B. 神经底座与工程链路 | 已完成 | 无标签训练、正交、checkpoint、CSV、manifest 和哈希可运行 |
| C. P4 目标筛选 | 已完成，STOP | anchor 有用，但低频 ROM 的额外收益需要因果控制 |
| D. P5 机制归因 | 已完成，STOP | 36/36 run、0 失败；机制归因和 gap 安全均失败 |
| E. P5 独立证据审计 | 已完成，PASS | checkpoint、CSV、JSON、manifest、SHA 与 gate 可重算一致 |
| F. M4 本机工程 smoke | 已完成，PASS | MPS 12/12 run；只证明开发环境可用 |
| G. 初步失效风险探针 | 已完成一轮 | 简单 residual/谱隙近似无效，Gram 只有弱信号 |
| H. 独立风险开发集与组合风险量 | **已完成，GO** | audit AUROC 0.869，全部冻结门槛通过 |
| **I. 条件谱簇校正器** | **当前应设计** | 尚未实现，不能直接上大规模 GPU |
| J. 3-seed CUDA/ROCm 决策 pilot | 未运行 | 只有 I 工程 smoke 通过后才租 GPU |
| K. promotion 与 frozen final | 锁定 | pilot GO 前禁止运行 |
| L. 完整论文与投稿 | 未达到 | 当前只有研究骨架和负结果，不是可投稿稿件 |

所以当前准确位置是：**P5 STOP 保持不变，P0 风险 GO，正在从 H 进入 I。**

## 4. 已经得到的权威结果

### P5：AMD MI300X / ROCm

- 6 方法 × 2 势族 × 3 seeds = 36/36 run；
- 0 失败，有限性、正交和 Gram 稳定性通过；
- 证据 SHA-256：
  `56891c5740eb94c62299b72869d790a1cbdee6abda3fae2903ed822f5e405101`；
- 独立审计：`audit_pass=true`；
- 最终判定：`P5_PROMOTION_STOP`。

| 方法 | near-cluster | gap-scan | 解释 |
|---|---:|---:|---|
| 低频 ROM 候选 | 0.11018 | 0.14920 | 局部有效，但未通过整体门槛 |
| 基础 anchor | 0.12323 | **0.14013** | gap-scan 最安全的非 ROM 基线 |
| wide-anchor | 0.11910 | 0.15098 | 参数量匹配控制 |
| long-anchor | **0.10616** | 0.15930 | near 反超候选，但只是更长训练 |
| 无 anchor 低频 ROM | 0.17171 | 0.21828 | 去掉 anchor 后明显退化 |
| 高频 ROM | 0.12110 | 0.14783 | 频率结构控制 |

低频 ROM 相对基础 anchor 的 near 误差改善 10.58%，但相对等算力 long-anchor 退化
3.79%，gap-scan 又比基础 anchor 回退 6.47%。因此不能把提升归因于低频结构。

### Apple M4 / MPS 工程 smoke

- 6 方法 × 2 势族 × 1 seed = 12/12 run；
- 每个方法 5 steps；
- 核心运行约 1.42 秒；
- 最大正交误差 `3.08×10⁻⁷`；
- 状态 `SMOKE_PASS`。

MPS 不支持正式复核需要的 float64 与原生复数 QR，因此该结果不能替代 CUDA/ROCm
论文结果。

### 初步风险探针

低频 ROM 相对 anchor 在 192 个严格配对点中退化 75 次，失败率 39.1%；其中
gap-scan 退化率为 75%。

| 风险量 | AUROC | AP | 判定 |
|---|---:|---:|---|
| PDE residual | 0.541 | 0.385 | 接近随机 |
| Gram condition | 0.583 | 0.535 | 只有弱信号 |
| 外部谱隙倒数 | 0.411 | 0.340 | 无效 |
| 内部谱隙倒数 | 0.457 | 0.354 | 无效 |

这说明不能直接使用“谱隙小就回退”或“residual 大就回退”的简单规则。

### P0 独立风险开发

- 160 个与 validation/frozen final 零重叠的新参数点；
- calibration/audit 各 80 点，12 个已审计 checkpoint，480 个配对行；
- reference cutoff 24、rank 3、33×33 grid；
- regression AUROC `0.869154`，95% CI `[0.818069, 0.912911]`；
- unsafe AUROC `0.843848`；
- harmonic / Gaussian AUROC `0.968532 / 0.716611`；
- top-20% precision `0.916667`；
- 80% coverage 时 unsafe rate `0.425000 → 0.307292`；
- 证据与独立重算通过，状态 `RISK_DEVELOPMENT_GO`。

限制：parameter-only 风险基线 AUROC 为 `0.707734`。组合特征高约 0.161，但风险具有
参数区域结构；P1 必须把 parameter-only 作为强基线，不能宣称风险完全来自物理残差。

## 5. 下一步必须完成什么

### P0：已完成——风险可检测性

套件、参考、特征、calibration/audit、gate 与证据包已经完成并独立复核。P0 不再调参，
不把 audit 重新用作 P1 的训练集。

### P1：当前下一步——条件谱簇校正器

目标结构：

1. anchor 网络提供默认稳定输出；
2. parameter-only、单特征和组合风险量做公平风险基线；
3. 只有开发集证明“局部校正可靠”的区域启用校正；
4. 高风险或未覆盖区域回退 PWE；
5. 阈值只在 risk-development 上冻结，不能在 final 上调整。

公平对照必须包括 anchor、long-anchor、wide-anchor、static-low-ROM、trace/Ky Fan
神经子空间和传统 PWE。

#### P1 成功线

- near-cluster 相对 long-anchor 至少改善 5%；
- gap-scan 不超过最佳非 ROM 基线 2%；
- 两势族均改善，至少 5/6 势族×seed 配对获胜；
- 参数量和 wall-clock 两种预算下都公平；
- 报告回退率、推理时间和 many-query break-even；
- 所有 run、失败、checkpoint、CSV、manifest 和 SHA 均封存。

### P2：P1 通过后——单卡决策实验

- 本地 M4 或 RTX 4060：5–50 step 工程 smoke；
- 单张 RTX 4090/5090：2 势族 × 3 seeds × 500–1000 steps 决策 pilot；
- 不需要多卡；
- pilot GO 后才补强正式基线、消融、泛化和 promotion；
- promotion GO 后才运行一次 frozen final。

## 6. 还缺哪些具体东西

### 代码

- 条件校正/回退路由；
- 相同 wall-clock 预算控制器；
- P1 新协议测试、断点续训、证据打包和独立审计器。

### 数值与实验

- 参考 PWE 的 symmetry-closed cutoff 收敛复核；
- 两势族独立开发集；
- 至少 3 个随机种子；
- Wang–Xie/trace 子空间及另一强神经基线；
- 参数量、训练时间、推理时间、显存与 break-even；
- 消融、OOD、噪声、训练点数、网格和 float64 复核。

### 论文证据

- 真实 final 主结果表；
- 风险—覆盖与校准图；
- k 平面 projector-error 热图；
- 两势族 seed 配对分布；
- 条件校正与 fallback 消融；
- 精度—成本 Pareto；
- 可选 projector quantum metric，下游结果未运行前不能预设有效。

## 7. 投稿前要达到哪里

### SCI 四区最低候选线

- 新机制通过 P0/P1/P2；
- 两势族、3+ seeds、强基线和公平预算；
- promotion 与一次 frozen final；
- 主实验、消融、泛化、鲁棒性、效率和失败案例完整；
- 代码、配置、CSV、图、checkpoint 与环境可公开复现。

### SCI 三区冲刺线

除四区条件外，还需要至少一个明显补强：

- 可解释且经过验证的风险/误差理论；
- 可靠的参数外泛化与 many-query 实用优势；
- projector quantum geometry 等下游物理价值；
- 或在第二类 PDE 本征问题上证明方法不是只对单个方程有效。

当前距离四区候选仍差 P1、单卡 pilot、promotion 与 final；距离三区更远。

## 8. 论文写作框架

暂不冻结算法名。若条件机制通过，可暂用标题：

> Failure-Aware Label-Free Neural Spectral-Cluster Solvers for Parametric
> Bloch–Schrödinger Eigenproblems

建议章节：Introduction；Related Work；Problem Formulation；Baseline Neural Solver；
Failure-Aware Conditional Correction；Analysis；Experimental Protocol；Results；Discussion；
Conclusion。

必须明确承认 Ky Fan、trace、Grassmann、Gram–Schmidt、Bloch ROM、PWE fallback 和
projector quantum geometry 均不是本文单独发明。

## 9. 必须准备的论文图片

1. PDE—anchor—风险—校正—fallback 总流程；
2. 两势族势场、Dirac 能带及内外谱隙；
3. k 平面参考、预测与 projector error 热图；
4. IID/exact/near/OOD/gap 的 violin/box 和 seed 配对线；
5. 风险—覆盖曲线、PR/ROC 与校准图；
6. 失败点和 gap-scan 退化区域；
7. 风险模块、校正模块和 fallback 消融；
8. 参数量、时间、显存和精度 Pareto；
9. many-query break-even；
10. cutoff、网格、训练点和 float64 收敛；
11. 只有真实完成后才加入 projector quantum metric。

每张图必须绑定生成脚本、原始 CSV、seed、硬件、指标定义和 SHA-256。

## 10. 算力准备

| 工作 | 设备 | 数量 |
|---|---|---:|
| 文献、风险分析、绘图 | Apple M4 / CPU | 无需租卡 |
| 工程 smoke | MPS 或 RTX 4060 8GB | 1 |
| 3-seed 决策 pilot | RTX 4090 24GB 或 5090 32GB | 1 |
| float64 关键复核 | 同一张高端消费卡；必要时短租 A100 | 1 |

云端关机前必须回传 checkpoint、CSV、环境、日志、manifest 和 SHA sidecar。不要提前租
多卡或 H100。

## 11. 当前禁止事项

- 不修改 P5 门槛重跑；
- 不挑 best checkpoint 替换 frozen final 规则；
- 不把 long-anchor 或更多训练步数包装成创新；
- 不在 P1 GO 前运行完整矩阵；
- 不打开 `evaluate_v2_final.py`；
- 不把 validation、MPS smoke、理论预期或 PWE 探针写成论文 final 结果；
- 不先定期刊再倒推结果。

## 12. 权威文件入口

- 通俗页面：`00_项目总览/03_研究进展与实验结果.html`；
- 本地权威代码：`03_当前主线_BlockKyFanPINN/02_当前代码仓库/`；
- GitHub：<https://github.com/Lazywords2006/PINN-PDE>；
- P5 独立审计：代码仓库 `docs/P5-INDEPENDENT-AUDIT.zh-CN.md`；
- P5 权威证据：代码仓库 `artifacts/p5-evidence-20260801-092048.tar.gz`；
- P0 风险证据：代码仓库 `artifacts/risk-development-evidence-20260824-083955.tar.gz`；
- P0 运行手册：代码仓库 `docs/RISK-DEVELOPMENT-RUNBOOK.zh-CN.md`；
- 当前文献索引：`01_文献库/README.md`。

## 13. 最终决策

- 继续研究：**YES**；
- 继续低频 ROM 主线：**NO**；
- P0 风险可检测性：**GO**；
- 立即运行大规模 GPU：**NO**；
- 当前投稿：**NO**；
- 当前唯一任务：**冻结 P1 条件校正器设计与公平小测协议。**
